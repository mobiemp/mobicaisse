import * as React from 'react';
import {Button } from 'react-native';
import ClotureForm from '../Components/clotureForm';

const ClotureScreen = ({ navigation }) => {
    return (
      <ClotureForm />
    );
  };
  
  export default ClotureScreen;